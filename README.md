# Find Duplicates

A simple script to find duplicate attributes in a profile.
It uses coroutines to speed up the process, and scale to a large number of attributes.

### Usage

##### Download dependencies
```sh
pip install -r requirements.txt
```

##### Run the file
```sh
python main.py
```